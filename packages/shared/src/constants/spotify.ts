/**
 * Cross-app constants for the Spotify domain.
 */

/** How often the header player re-polls `/me/player` while the panel is open. */
export const SPOTIFY_POLL_INTERVAL_MS = 5000;

/** Max track results the search box requests/shows. */
export const SPOTIFY_SEARCH_LIMIT = 10;
