/**
 * Tab-related constants shared across the main process and renderer.
 */

/** Maximum Claude chat tabs open at once per workspace/worktree. */
export const MAX_TABS_PER_WORKSPACE = 5;

/** Title given to a freshly-opened tab. */
export const DEFAULT_TAB_TITLE = 'Chat';
